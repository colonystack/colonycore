# Test Fixtures

This directory holds curated fixtures used by CLI and contract tests.

- `registry/`: governance registry schema fixtures for `cmd/registry-check`.
- `dataset-templates/`: dataset template fixtures for `cmd/colony lint dataset`.
- `entity-model/`: generated entity-model snapshot fixtures for persistence/rules parity tests.

Use the nearest fixture-specific README for authoring rules and validation commands.
